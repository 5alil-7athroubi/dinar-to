{
  "name": "backend",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",  // Used for production environment in Nixpacks
    "dev": "nodemon server.js", // For local development, ensure you have `nodemon` installed
    "build": "echo 'No build steps defined yet'" // Optional: Add if you need a custom build step
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "Backend for DINAR app",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.8.1",
    "multer": "^1.4.5-lts.1"
  }
}
